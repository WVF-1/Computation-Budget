# Computation-Budget
An attempt to create a new scheduler which can be implemented across graph network architectures in order to best understand a computational budget as well as reduce ineffiencies while maintaining accuracy.
